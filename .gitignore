# Cruft específico de la máquina (no forma parte del rice)
config/fish/fish_variables
config/systemd/user/*.target.wants/

# Backups que crea el instalador (~/.config/<name>.bak.*) y locales
*.bak
*.bak.*
